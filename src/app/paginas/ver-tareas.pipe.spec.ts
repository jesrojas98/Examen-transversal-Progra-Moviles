import { VerTareasPipe } from './ver-tareas.pipe';

describe('VerTareasPipe', () => {
  it('create an instance', () => {
    const pipe = new VerTareasPipe();
    expect(pipe).toBeTruthy();
  });
});
